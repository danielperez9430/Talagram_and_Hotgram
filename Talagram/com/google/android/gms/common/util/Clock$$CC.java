package com.google.android.gms.common.util;

import android.os.SystemClock;

public class Clock$$CC {
    public static long currentThreadTimeMillis(Clock arg2) {
        return SystemClock.currentThreadTimeMillis();
    }
}

